'use client';

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ModuleAccess() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Module Access</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Add module access content */}
      </CardContent>
    </Card>
  );
}